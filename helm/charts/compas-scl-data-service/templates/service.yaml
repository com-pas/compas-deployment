apiVersion: v1
kind: Service
metadata:
  name: {{ include "scl-data-service.fullname" . }}
  labels:
    {{- include "scl-data-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scl-data-service.selectorLabels" . | nindent 4 }}
